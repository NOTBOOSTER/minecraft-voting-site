<!DOCTYPE html>
<html>
<head>
    <title>Vote Successful</title>
    <meta http-equiv="refresh" content="2;url=/">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --primary: #ff3333;
            --dark: #1a1a1a;
        }

        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: var(--dark);
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            text-align: center;
            padding: 2rem;
        }

        .container {
            background: rgba(0, 0, 0, 0.9);
            padding: 2rem;
            border-radius: 8px;
            border: 2px solid var(--primary);
            box-shadow: 0 0 20px rgba(255, 51, 51, 0.2);
            max-width: 600px;
        }

        h1 {
            color: #4CAF50;
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        p {
            font-size: 1.2rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Vote Successful!</h1>
        <p>Thank you for your vote. Redirecting you back...</p>
    </div>
</body>
</html>
