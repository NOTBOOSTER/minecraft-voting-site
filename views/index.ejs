<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minecraft Server Voting</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Minecraft&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #ff3333;
            --secondary: #990000;
            --dark: #1a1a1a;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: var(--dark);
            background-image: 
                radial-gradient(circle at 25px 25px, rgba(255, 51, 51, 0.2) 2%, transparent 0%),
                radial-gradient(circle at 75px 75px, rgba(255, 51, 51, 0.15) 2%, transparent 0%);
            background-size: 100px 100px;
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            margin: 0;
        }

        .main-content {
            width: 100%;
            max-width: 800px;
            background: rgba(0, 0, 0, 0.9);
            padding: 2rem;
            border-radius: 8px;
            border: 2px solid var(--primary);
            box-shadow: 0 0 20px rgba(255, 51, 51, 0.2);
            text-align: center;
        }
        button {
            width: 100%;
            padding: 1rem;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 1.1rem;
            cursor: pointer;
            text-transform: uppercase;
            font-weight: bold;
        }

        button:hover {
            background: var(--secondary);
            transform: translateY(-2px);
        }
        .sites{
            background-color: #1a1a1a;
            border-radius: 12px;
            border: 1px solid var(--primary);
        }
        
    </style>
</head>
<body>
    <div>
        <div class="main-content">
            <h1 class="text-4xl font-bold text-center text-white mb-8">Vote for Our Server</h1>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <% for(let i = 1; i <= 4; i++) { %>
                <div class="sites p-6 rounded-lg shadow-lg hover:transform hover:scale-105 transition duration-200">
                    <h2 class="text-2xl font-bold text-white mb-4">Voting Site #<%= i %></h2>
                    <p class="text-gray-300 mb-4">Vote on this site to receive amazing rewards!</p>
                    <a href="/vote/<%= i %>">
                        <button>
                            Vote Now
                        </button>
                    </a>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>