<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minecraft Server Voting</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Minecraft&display=swap" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
</head>
<body>
    <div>
        <div class="bg-gray-900 min-h-screen flex justify-center items-center py-6 px-4">
            <div class="rounded-lg p-6 bg-gray-800 border-2 border-gray-600 ">
                <h1 class="text-4xl font-bold text-center text-white mb-8"><%= name %></h1>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <% for(let i = 1; i <= 4; i++) { %>
                <div class="sites p-6 rounded-lg shadow-lg hover:transform hover:scale-105 transition duration-200 bg-gray-900">
                    <h2 class="text-2xl font-bold text-white mb-4">Voting Site #<%= i %></h2>
                    <p class="text-gray-300 mb-4">Vote on this site to receive amazing rewards!</p>
                    <a href="/vote/<%= i %>">
                        <button class="w-full px-4 py-2 bg-red-500 hover:bg-red-700 text-white border-none rounded-md text-xl cursor-pointer uppercase font-bold transition-transform transform hover:translate-y-[-2px]">
                            Vote Now
                        </button>
                    </a>
                </div>
                <% } %>
            </div>
            <%- adistraurl %>
            <%- adistraredirect %>

            <%- include('./leaderboard') %>
            </div>
        </div>
    </div>
    <%- include('_copyright') %>
</body>
</html>